# The project is edited on Windows but runs in a Linux container, so keep
# everything LF in the repository regardless of the checkout platform.
* text=auto eol=lf
*.png binary
*.jpg binary
*.epub binary
